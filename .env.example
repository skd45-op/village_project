# ---- Database (Supabase Postgres) ----
# App runtime connection — use the Supabase "Connection pooling" (Transaction) URL.
DATABASE_URL="postgresql://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true"
# Direct connection (port 5432) — used by Prisma Migrate / introspection.
DIRECT_URL="postgresql://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres"

# ---- Supabase Auth / Storage (client-safe) ----
NEXT_PUBLIC_SUPABASE_URL="https://<ref>.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="<anon-public-key>"

# ---- Server-only (never expose to the browser) ----
SUPABASE_SERVICE_ROLE_KEY="<service-role-key>"

# Secret gate for the one-time /bootstrap page that creates the first Super Admin.
BOOTSTRAP_SECRET="<a-long-random-string>"
